import { type CodeLayoutGrid } from "./CodeLayout";

/**
 * Default accept grid names for panel drag and drop.
 */
export const defaultAccept : CodeLayoutGrid[] = [ 'bottomPanel', 'primarySideBar','secondarySideBar' ];
